<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>message miniboard</title>
</head>
<body>
    <h1>Messages</h1>
    <% messages.forEach((message, _, index)=>{ %>
        <section style="margin-bottom: 5vh;">
            <h2><em>Authors Name:</em> <%= message.user %></h2>
            <p><em>Message:</em> <%= message.text %> </p>
            <a href="/messages/<%= message.id %>">Message Details</a>
            <p><em>Date Added:</em> <%= message.added %></p>
        </section>
    <% }) %>
    <a href="/new"><h3>Create New Message</h3></a>
</body>
</html>